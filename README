Personal configuration for vim. To use it, clone this repository to ~/.vim and
add following line to ~/.vimrc:
    :source ~/.vim/profile

Features:
 * wombat colorscheme
 * ,l shortcut for quickly switching between buffers
 * ,n shortcut for NERD_Tree
 * enhanced syntax highlight for html, JavaScript and Vala
 * pyflakes and python rope included
